#include <stdio.h>
/**
 * main - number of arguments passed into it"
 * @argc: parameters of number of arguments
 * @argv: paramters two
 * Return: int
 */

int main(int argc, char *argv[]__attribute__((unused)))
{

	printf("%d\n", argc - 1);

	return (0);
}
