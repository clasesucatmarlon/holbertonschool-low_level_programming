#include <stdio.h>
/**
 * main - prints its name
 * @argc: receive numbers
 * @argv: arrayi
 * Return: 0
 */

int main(int argc, char *argv[])
{

	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
